<?xml version="1.0" encoding="utf8" ?>
<feed xmlns="http://www.w3.org/2005/Atom">
	<title type="text">Facebook export for {{user.name}}</title>
	<author>
		<name>{{user.name}}</name>
	</author>

	{% for entry in feed %}
	<entry>
		<id>http://fb-export.appspot.com/id/{{entry.type}}/{{entry.id}}</id>
		<category term="{{entry.type}}"/>
		<title>{{entry.title}}</title>
		{% if entry.created %}<published>{{entry.created}}</published>{% endif %}
		{% if entry.updated %}<updated>{{entry.updated}}</updated>{% endif %}
		{% if entry.url %}<link href="{{entry.url}}" />{% endif %}
		<author>{{user.name}}</author>
		{% if entry.summary %}<summary type="html">{{entry.summary}}</summary>{% endif %}
		{% if entry.content %}<content type="html">{{entry.content}}</content>{% endif %}
	</entry>
	{% endfor %}
</feed>

